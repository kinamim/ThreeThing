<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ThreeThing" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Required to stop crashes in Compose -->
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.ThreeThing.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.ThreeThing</item>
    </style>
</resources>
